# app/constants.py

# All supported model names as used in the UI & inference
MODEL_TYPES = ["ARIMA", "SARIMAX", "LSTM", "TCN", "TRANSFORMER"]

# Sub-groups for logic branches
TIME_SERIES_MODELS = ["ARIMA", "SARIMAX"]
DL_MODELS = ["LSTM", "TCN", "TRANSFORMER"]
